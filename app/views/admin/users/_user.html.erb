<li>
  <%= user.id %>
  <%= link_to user.name, admin_user_path(user), class: "unit_user" %>
  | <%= link_to t("delete"), admin_user_path(user), method: :delete,
    data: {confirm: t("are_you_sure")} %>
</li>
