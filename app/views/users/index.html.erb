<% provide :title, t("all_users") %>
<h1><%= t "all_users" %></h1>
<div class="list_user">
<%= render "shared/flash" %>

<div class="list-users">
  <%= paginate @users, theme: "twitter-bootstrap-3" %>

  <ul>
    <%= render @users %>
  </ul>

  <%= paginate @users, theme: "twitter-bootstrap-3" %>
</div>
